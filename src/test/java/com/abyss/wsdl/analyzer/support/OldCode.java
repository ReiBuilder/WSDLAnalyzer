package com.abyss.wsdl.analyzer.support;

/**
 * Created by lblsloveryy on 15-2-10.
 */
public class OldCode {
    //tset
    /*
    int length1 = parametersList.size();
    for(int i = 0; i<length1; i++)
    {
        System.out.println(i + " parameter is " + parametersList.get(i).getParamName()
                + ". the owner id is " + parametersList.get(i).getOwnerId()
                + ". the parameter type is " + parametersList.get(i).getTypeName()
                + ". the parameter class is " + parametersList.get(i).getClassName());
    }

    System.out.println("-----------the bridge---------------");
    int length2 = bridgeList.size();
    for(int i = 0; i<length2; i++)
    {
        System.out.println(i + " elementName is " + bridgeList.get(i).getElementName()
                + ". the owner id is " + bridgeList.get(i).getOwnerId()
                + ". the parameterType is " + bridgeList.get(i).getParamType()
                + ". the messageName is " + bridgeList.get(i).getMessageName());
    }

    System.out.println("-----------the operation---------------");
    int length3 = operationList.size();
    for(int i = 0; i<length3; i++)
    {
        System.out.println(i + " operation Name is " + operationList.get(i).getOperationName()
                + ". the owner id is " + operationList.get(i).getOwnerId()
                + ". the operation Id is " + operationList.get(i).getId());
    }

    System.out.println("-----------the operation-parameter---------------");
    int length4 = opList.size();
    for(int i = 0; i<length4; i++)
    {
        System.out.println(i + " operation id is " + opList.get(i).getOperationId()
                + ". the parameter id is " + opList.get(i).getParameterId());
    }
    */
}
